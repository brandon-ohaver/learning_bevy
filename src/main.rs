use bevy::prelude::*; // imports the essential needs from Bevy

// Every Bevy program can be referred to as a 'App'
// Using the App struct, we can call new() on it to create a new Bevy program and run() to run it
fn main() {
    App::new().run();
}
